# Copy to /opt/shared-redis/.env (next to compose.yaml).
# Do not commit the real file.

SHARED_REDIS_IMAGE=redis:7-alpine
SHARED_REDIS_HOST_PORT=127.0.0.1:6379

SHARED_REDIS_NETWORK=shared-redis-net
SHARED_REDIS_NETWORK_SUBNET=172.31.0.0/24
SHARED_REDIS_VOLUME=shared-redis-data
